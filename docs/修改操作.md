# 彭
2025年11月23日
1. 将file_utils.py移动到了utils文件夹内，目前搜索全局，暂时没有代码引用了该代码里的函数。
2. 目前修改集中于给planner加入一个识别文件的功能，planner可以由此明确input，而不是局限于用户的文本输入
3. 在`src/control`文件夹内加入`console.py`和`backend.py`，分别负责控制台交互（管理Planner、Workers、Reporter）和后端逻辑处理
4. 在`src/frontend`文件夹搭建了一个简单的前端框架

2025年12月1日：
1. input_files下文件更新为最新版（文件名都未变， 变的只有内容）

# 商
2025年11月24日
1. 在src/tools/ 文件夹中新建 trial/ 文件夹存放一些新代码
2. 在trial文件夹新增tools_matcher.py和tools_generator.py文件；其中包括两个类，ToolsMatcher和ToolsGenerator，前者负责根据上游的任务从已有工具库返回所需工具或者调用生成器生成没有的工具；后者负责生成。

# ...... 