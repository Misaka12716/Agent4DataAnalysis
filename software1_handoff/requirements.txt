# Software 1 算子框架交付包 — 精简依赖清单
# (Last verified 2026-05-12 against the operator implementations under
#  distillation/software1_solver/solvers/.)
#
# 安装:
#   python -m venv .venv
#   source .venv/bin/activate   # Windows: .\.venv\Scripts\activate
#   pip install -r handoff_requirements.txt
#
# Python: 推荐 3.10 或 3.11.

# --- 算子层（必装，覆盖全部 32 个算子）---
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
lifelines>=0.27
mlxtend>=0.22

# --- LLM agent / mapping engine ---
requests>=2.31
python-dotenv>=1.0

# --- Pipeline UI demo (Flask) ---
flask>=3.0

# --- 可选 ML（未装则对应算子调用时抛清晰报错，其他 30 个不受影响）---
xgboost>=2.0
lightgbm>=4.0

# --- 中文文本特征（jieba）算子可选依赖 ---
jieba>=0.42
