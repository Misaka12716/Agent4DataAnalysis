# 使用说明：复制本文件为 `.env` 后填写密钥与地址（`.env` 已在 .gitignore 中，勿提交仓库）
#   copy .env.example .env
#
# 以下变量供 `python -m distillation.scripts.distill_ten_opus` 等脚本读取（启动时 load_dotenv）

# --- Anthropic Claude（蒸馏教师）---
ANTHROPIC_API_KEY=
# API 根地址，不要带末尾斜杠；官方默认如下。若走企业代理 / 兼容网关，改为你的网关主机根 URL（仍须兼容 Anthropic Messages API 路径 /v1/messages）
ANTHROPIC_API_BASE_URL=https://api.anthropic.com

# --- OpenAI 兼容 Chat（网关上的 Llama-3-8B 等，不下载本地权重）---
# 与 Anthropic 可共用同一网关主机时，可只填模型名，BASE 与 KEY 复用下方 Anthropic 变量。
# OPENAI_API_BASE_URL=https://api.example.com
# OPENAI_API_KEY=
# 模型名须与网关 GET /v1/models 返回的 id 一致（示例为 agicto 上的 Llama-3-8B）
# DISTILL_OPENAI_MODEL=llama-3-8b-chat-hf
# DISTILL_OPENAI_JUDGE_MODEL=llama-3-8b-chat-hf
# DISTILL_TEACHER_BACKEND=openai

# --- 本地 HF 教师（可选，需 torch + transformers）---
# 默认与 Meta 官方权重名一致；若本机目录名为 Llama-3-8b-chat-hf，可改为该路径
# 默认脚本使用公开镜像；官方权重需先在 HF 接受许可并设置 HF_TOKEN
# HF_DISTILL_MODEL=AgentPublic/llama3-instruct-8b
# HF_DISTILL_MODEL=meta-llama/Meta-Llama-3-8B-Instruct
# HF_MAX_NEW_TOKENS=2048
# HF_TEMPERATURE=0.2
# 或环境变量 DISTILL_TEACHER_BACKEND=hf，等价于命令行 --teacher-backend hf
